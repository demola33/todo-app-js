{
  "name": "todo-app",
  "version": "1.0.0",
  "description": "A basic todo application with vanilla javascript",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass:build": "sass --no-source-map scss:css",
    "sass:watch": "sass --no-source-map --watch scss:css"
  },
  "author": "Ademola Akanmu",
  "license": "MIT",
  "dependencies": {
    "@fortawesome/fontawesome-free": "^6.4.2",
    "bootstrap": "^5.3.1",
    "sass": "^1.66.1"
  }
}
